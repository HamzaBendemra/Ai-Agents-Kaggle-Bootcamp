# Example Project

This is an example mini-project to demonstrate the repository structure.

## Setup

```bash
# Install dependencies
poetry install

# Activate virtual environment
poetry shell
```

## Running

```bash
poetry run python src/main.py
```

## Adding Dependencies

```bash
# Add a runtime dependency
poetry add requests

# Add a development dependency
poetry add --group dev pytest
```
